services:
  - docker

script:
  - mkdir _site/
  - chmod 777 _site/
  - make buildimage build

deploy:
  provider: pages
  local-dir: _site
  skip-cleanup: true
  github-token: $GITHUB_TOKEN 
  keep-history: true
  on:
    branch: master
